dependencies {
    implementation project(':service')
}
// language level は release 優先の discovery 規則を検証する
// (java-analyzer feature doc「Source root discovery と解析 context」)。
tasks.named('compileJava') { options.release = 17 }
