# P5_01: 修正後の再計測・scope 外記録・sync 締めを行う

## 絶対ルール

- spec に明記された範囲だけを対象にする
- 不明点は推測で埋めず、停止してユーザーに確認する
- 参照 path を外れて広く探索しない (Grep / Glob / 既存実装の探索は禁止)
- 別 app / package を追加探索せず、この prompt 内の情報だけで判断する
- 各作業ステップに含まれる検証 / レビュー手順をスキップしない
- レポート・spec・commit・issue には集計値のみを記録する
- **完了条件のタスク化**: 作業開始前に「完了条件」セクションの各項目を todo として登録し、各ステップ完了時に状態を更新すること。タスク化せずに作業を開始することは禁止

### 実装アンチパターンの回避 (必守)

- スコープ厳守: spec / 本 prompt に明記された機能のみ実装する。未要求の機能追加・
  先回りの抽象化・無関係なリファクタ・暗黙の互換維持をしない。
- 既存規約への整合: 命名・エラー処理・ログ・テスト・API 連携方式は、対象コードベースの
  既存パターンに合わせる。新方式を持ち込む場合は理由を述べて確認を取る。
- 観測可能な契約の保持: UI 文言・イベント名・戻り値・エラーメッセージ・ログ形式・API を
  要求なく変更しない。変更が必要なら理由と影響を明記する。
- 推測の排除: 要件・業務ルール・API 仕様が不明なら停止して確認する。それらしいが
  誤った実装 (存在しない API 呼び出し / 非互換な引数) を避け、import と API の実在を確認する。
- fallback の最小化: `??` / `||` / 既定引数 / 多段 fallback / 暗黙のエラー握り潰しは
  「任意データ」に限定する。必須データの欠落は隠さず明示的に失敗させる。
- 過剰実装の排除: 単純な条件分岐を strategy / handler map に置換しない。
  要求も計測もない caching / memoization を入れない。
- dead code を残さない: 到達不能コード・未使用の変数 / 関数 / import / export・
  変更後に不要化した型定義を削除する。
- 判断の記録: 非自明な設計判断は理由 (or spec / ADR へのリンク) を残す。

## 作業ステップ (この順序で実行する)

### ステップ 0: ブランチ準備

ブランチ命名と base branch は `AGENTS.md` の `Spec Workflow Contract` および `workflow-git` に従う。

1. 作業ブランチ `feature-27` が checkout 済みで、P4_01〜P4_04 がすべて完了していることを確認する
2. Analyzer jar を rebuild する (`cd analyzers/java && ./gradlew shadowJar`)

### ステップ 1: 修正後の再計測

1. P2_02 と同一条件 (同一 commit・同一実行環境) で実プロジェクト 2 件を再計測する
2. 要因クラス別の件数推移 (修正前 → 修正後) を `report.md` の「再計測結果」節へ記録する
3. 修正対象クラスの残存が想定より多い場合は、原因を整理してユーザーへ報告し、追加対応の要否を確認する

### ステップ 2: v1 scope 外記録と ADR-0004 判定

1. scope 外と判定されたクラスの残存件数・全体比を記録し、ADR-0004 の再検討条件 (「静的解析で主要ユースケースの精度を満たせない」「未解決が支配的」) に抵触するかを判定する
2. **判定案をユーザーへ提示して停止し、承認を得る**
3. 承認結果に従い: 追記要なら ADR-0004 の `## 決定` 節の観測可能性の方針へ診断 metadata による要因クラス観測を追記し、ADR のメタ情報を同期する。追記不要ならその判断理由を spec へ記録する
4. spec の `## 上位資料からの変更点` の ADR-0004 行を反映済み or 不要 (理由付き) へ更新する

### ステップ 3: spec / issue の締め

1. spec の設計フェーズ状況・変更履歴・メタ情報を最終状態へ同期する (実装分割 → 完了 / レビュー済)
2. issue #27 の完了条件チェックリストを満たしているか突合し、満たした項目の根拠 (再計測値・commit) を issue コメントとして投稿する
3. diff レビューを回し、指摘を対応する

### ステップ最終: 最終確認

1. 全テスト / E2E / 健全性検査がパスすることを確認
2. PR を Ready に変更しレビュアーを指名する
3. `context-harvest` で横断知見 (実測から得た JavaParser 限界の傾向等) の書き戻し要否を判断する

## 実装コンテキスト

- spec: `specs/27-unresolved-call-diagnosis/index.md`
- `specs/27-unresolved-call-diagnosis/report.md` (P2_02 / P3_01 の記録。追記対象)
- `adr/0004-defer-runtime-call-tracing.md` (条件付き追記先)
- issue #27 の完了条件 (2026-07-21 のスコープ変更コメント)
- repo root: `$(ghq root)/github.com/Fukuemon/depwalk`
- 参照する path:
  - `specs/27-unresolved-call-diagnosis/` (spec / report / prompts)
  - `adr/0004-defer-runtime-call-tracing.md`

## 前提条件

- 完了しているべき phase / 依存 prompt: `P4_01` / `P4_02` / `P4_03` / `P4_04` すべて
- 完了後に着手可能になる後続 prompt: なし (最終 prompt)
- 必要な repo 状態: 修正込み Analyzer jar が build 可能。P2_02 と同一の実測対象へ到達可能な実行環境 (ユーザー確認)

## 不明点ハンドリング

- 矛盾 / 欠落 / 未定義を見つけたら作業を止める
- 推測で実装を進めない
- ADR-0004 の追記要否は必ずユーザー承認を得る (独断で ADR を変更しない)
- 再計測条件が P2_02 と揃えられない場合は、差異と影響を提示して停止する

## タスク境界

### 実装する範囲

- 修正後再計測と件数推移の記録
- v1 scope 外記録と ADR-0004 追記要否の判定・承認済み反映
- spec / issue のメタ情報最終同期と PR Ready 化

### 実装しない範囲

- 新たなコード修正 (残存が多い場合も勝手に修正せず、ユーザーへ追加対応を確認)
- ADR の新規起票
- 後続 issue の起票 (D5 改訂により行わない)

## 設計仕様

spec 成功条件 (抜粋):

> 「修正」判定の要因クラスが本 branch で修正され、最小再現 fixture の回帰検証と実プロジェクト再計測で件数減少が確認されている。JavaParser 上流限界による残存分は、仕様上の除外 / v1 scope 外として ADR-0004 との整合を明記して記録されている。

spec ADR 行 (上位資料からの変更点、抜粋):

> (条件付き・P9 で判定) JavaParser 上流限界による v1 scope 外記録が確定した場合、`## 決定` 節の観測可能性の方針へ診断 metadata による要因クラス観測を追記する。

## テスト観点

- 再計測が P2_02 と同一条件で行われ、件数推移が要因クラス別に記録されている
- 修正対象クラスの件数が減少している (減少しない場合は原因が記録されている)

## 検証コマンド

- `cd analyzers/java && ./gradlew test && ./gradlew shadowJar`
- `./analyzers/java/gradlew --no-daemon -p testdata/fixtures/java/spring-project clean writeDepwalkClasspath` (全体 E2E の事前 fixture build)
- `(cd core && DEPWALK_E2E_REQUIRED=1 go test ./e2e -count=1)` (required E2E)
- `cd core && go test ./... && go vet ./...`
- `lefthook run pre-commit`

## 完了条件

- [ ] ステップ 0 で P4 系全完了と jar rebuild を確認した
- [ ] 同一条件の再計測を完了し、件数推移を `report.md` へ記録した
- [ ] ADR-0004 の追記要否をユーザー承認のうえ反映 (or 理由付き不要記録) した
- [ ] spec の変更点テーブル (ADR 行) を最終状態へ更新した
- [ ] issue #27 の完了条件を突合し、根拠付きコメントを投稿した
- [ ] spec のフェーズ状況・変更履歴・メタ情報を最終同期した
- [ ] `## 検証コマンド` がすべてパスする
- [ ] PR を Ready に変更しレビュアーを指名した
- [ ] 未解決の仕様質問が残っていない
